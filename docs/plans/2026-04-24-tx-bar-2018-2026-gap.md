# Plan: TX Bar Discipline 2018-2026 Gap Closure

**Date:** 2026-04-24
**Branch:** `feat/tx-bar-2018-2026-gap-closure`
**Repo:** ImNotAnAttorney-web (worktree at `C:\Users\email\projects\tx-bar-work`)
**Handoff source:** `ImNotAnAttorney/docs/handoff/2026-04-24-tx-bar-2018-2026-gap.md`

## Why
Existing TX dataset covers 2014-2017 only (393 events from law.uh.edu PDFs; archive
stopped updating 2017). Powers Attorney-Vetting $47 SKU which needs balanced
2014-2026 coverage to match CA/FL/NY siblings. Target: +500-800 new events.

## Source
Texas Bar Journal monthly issues 2018+ at
`https://lsc-pagepro.mydigitalpublication.com/`. JS-rendered digital-magazine
viewer (HTTP 202 on direct fetch; same shape as CA bar Playwright pattern).

Probe order:
1. Inspect viewer DOM via Playwright headless on Sept 2025 known URL → look for
   downloadable PDF. If found → Route B (PDF + pdf-parse).
2. If not → Route A: `page.evaluate(() => document.body.innerText)` and parse
   with the existing BAR_RE regex.

## Files to create
1. `scripts/ingest/scrape-txbar-2018plus.mjs` — Playwright scraper, mirrors
   `scrape-txbar-discipline.mjs` regex + load path; new staging tables
   `_stg_attorneys_tx2018` / `_stg_discipline_tx2018` (suffix to avoid collision
   with any in-flight 2014-2017 reload).
2. `.tmp/probe-tbj.mjs` — throw-away DOM probe; deleted after smoke.

## Files to modify
None — additive only. No edits to existing TX 2014-2017 scraper.

## Numbered tasks
1. **Probe** TBJ DOM via `.tmp/probe-tbj.mjs` against the Sept 2025 known URL.
   Capture `document.body.innerText`, list iframe / download candidates.
2. **Decide route** (A: innerText extraction; B: PDF download + pdf-parse). Most
   likely A based on prior session findings.
3. **Discover archive** — Playwright nav to
   `lsc-pagepro.mydigitalpublication.com/publication/`, build map of monthly
   issue URLs 2018-01 → 2026-04 (~96 issues). Seed with the known 9 URLs from
   the handoff. Filter to issues that contain a "DISCIPLINARY ACTIONS"
   article anchor.
4. **Smoke test** — run the new scraper against ONE known issue (Sept 2025).
   Expect ≥5 valid records; confirm BAR_RE pattern matches the rendered text.
5. **Apply 1 issue** to staging — confirm DB row delta.
6. **Apply remaining issues** — chunked in groups of ~20 issues with commit-
   between-chunks. Polite delay 1-2s/req.
7. **Verify** — per-year histogram + source_url coverage + spot-check 1 URL.
8. **Plan summary** — append run results to this plan file. Commit on the
   feature branch.

## Hard constraints (per handoff prompt)
- Branch: `feat/tx-bar-2018-2026-gap-closure`. Worktree at
  `C:\Users\email\projects\tx-bar-work`.
- Use `bulkCopyRows` + `createBulkClient`. Per-row INSERT banned.
- Port 5432 (session mode) — handled by `createBulkClient`.
- File header MUST cite template + cl-bulk-data-defensive #18 + csv-bulk-checked.
- Real TX bar numbers required (existing 2014-2017 has bracketed bar# format).
- ON CONFLICT DO NOTHING idempotency on
  `(jurisdiction, bar_number, order_date, discipline_type)`.
- DO NOT touch 2014-2017 rows or other jurisdictions.
- DO NOT push or open a PR.
- DO NOT commit pdf-parse to package.json (install-on-demand).
- DO NOT background via nohup. DO NOT chunk single Bash >25 min.
- 100% `source_url` coverage — points to the actual TBJ issue URL.

## Polite scraping
- 1-2s delay/req with random jitter
- UA: `INAA-Crawler/1.0 (+https://imnotanattorney.com; contact: noreply-legal@inaa.com)`

## Exit criteria
- TX events post-load > 800 total (was 393)
- 100% source_url coverage (post-2018 rows)
- Per-year distribution exists for 2018-2026

## Cascade
- us: closes TX 9-year gap, completes CA/FL/TX/NY for ATTy-Vetting $47
- counterparty (Texas Bar): public records, polite delay, no scraping burden on
  texasbar.com; CDN host only
- future-us: scraper template reusable for any
  `lsc-pagepro.mydigitalpublication.com` magazine
- ecosystem: pattern publishable
- No node loses.

## Run results

**2026-04-24 — single full-run, ~13 min wall-clock, 92 issues / 96 attempted**

- Records collected: 1,083
- Attorneys upserted: 672
- Discipline events inserted (ON CONFLICT DO NOTHING dedup): 986
- TX total before: 393 events (2014-2017 only)
- TX total after: **1,389 events** (was 393, +996 new)
- source_url coverage: **100%** (1,389/1,389)
- Per-year distribution (post-load):
  ```
  2012: 1     2018: 133   2024: 82
  2013: 3     2019: 177   2025: 65
  2014: 54    2020: 113   2026: 13
  2015: 103   2021: 148
  2016: 143   2022: 132
  2017: 127   2023: 95
  ```
- Exit criteria all met:
  - TX events > 800 total: 1,389 (target 800)
  - source_url 100% on post-2018 rows
  - 2018-2026 per-year distribution exists

**Anomalies / future polish**
- One issue (May 2018, id=491612) returned only 1,969 chars and 0 entries — likely a viewer
  render hiccup. Re-running just that issue via `--issue-id 491612` would close the gap
  (~10 events expected).
- BODA-section entries (~10-20% of monthly content) use a prose shape that doesn't match
  BAR_RE; out-of-scope for v1. Future enhancement: add a second regex for BODA-style
  "of <city>, attorney <name> [#bar], <age>" entries (~+150-200 events estimated).
- 97 in-batch dups filtered by ON CONFLICT (BODA entries that recur as substantive
  discipline in later monthly issues).

**Smoke tests run**
1. Sept 2025 (id=850944) dry-run → 11 entries
2. Mar 2018 (id=478627) dry-run → 7 entries (Title Case names matched)
3. Sept 2025 apply → 10 events into DB (one in-batch dup collapsed)
4. Full run (92 issues) apply → 986 events
