# Accessibility Fixes, 4 Issues

**Spec:** Direct task from audit findings
**Repo:** C:\Users\email\projects\ImNotAnAttorney-web\
**Problem:** 4 WCAG violations: color-only selection indicators (2 components), missing focus management (mobile menu), missing aria-labels (admin pages)
**Key files:** ScoreClient.tsx, ChargeTypeSelector.tsx, Header.tsx, admin pages, OperatorShell.tsx
**Tech stack:** Next.js 15, React, Tailwind CSS
**Key decisions:** Use visual indicators (filled circles, checkmarks, bold text, border-width) beyond color for selection states. Use refs + useEffect for focus management.
**Setup:** None needed, pure frontend changes.

## Tasks

### Task 1: ScoreClient.tsx, Add non-color selection indicator to quiz radio buttons
- File: `src/app/score/ScoreClient.tsx`
- Replace native radio input with custom visual circle indicator (filled when selected)
- Add `font-medium` to selected option text
- Change `border` to `border-2` for selected state differentiation
- Hide native radio with `sr-only`

### Task 2: ChargeTypeSelector.tsx, Add checkmark icon to selected charge type
- File: `src/components/ChargeTypeSelector.tsx`
- Add checkmark SVG icon when `isSelected` is true
- Change `border` to `border-2` for thickness differentiation
- Add `font-bold` to selected text

### Task 3: Header.tsx, Mobile menu focus management
- File: `src/components/Header.tsx`
- Add ref to first mobile menu link
- Add useEffect to focus first link when `mobileOpen` becomes true

### Task 4: Admin/Operator aria-labels on password inputs and select elements
- Files: `src/components/OperatorShell.tsx`, `src/app/admin/partners/page.tsx`, `src/app/admin/inbox/page.tsx`, `src/app/admin/demand/page.tsx`, `src/app/operator/cases/page.tsx`, `src/app/operator/jobs/page.tsx`
- Add `aria-label` to all password inputs and select elements missing labels

### Task 5: Verify
- Run `npx tsc,noEmit` to confirm no type errors
