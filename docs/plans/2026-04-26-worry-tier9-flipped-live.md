# Worry-to-Pristine — 4 Tier 9 Just-Flipped Live Products

Audit date: 2026-04-26.
Repo: `C:/Users/email/projects/ImNotAnAttorney-web` (master @ `0533d171`).
Method: code grep + live PostgREST queries against prod Supabase + curl against `https://imnotanattorney.com`.

## Summary

- Audited: 4 products (district-court-intelligence $147, charge-authority-pack $97, precedent-watchlist $47, arrest-survival-kit $47).
- Passed end-to-end: 1 (arrest-survival-kit).
- BLOCKERS: 4 distinct CRITICALs across 3 products. Two products have no dedicated landing route + no AvailabilityChecker mount. One product's pre-purchase coverage gate returns `available:false` for every state.
- WARNINGs: 5. SUGGESTIONs: 3.

## Per-product matrix

| Product | Resolver | /<slug> page | /services/<slug> page | Checkout | Cron | Coverage gate | Verdict |
|---|---|---|---|---|---|---|---|
| district-court-intelligence | OK (uses ussc_districts.state_code) | 200 | 200 | OK | n/a | **BROKEN — returns 0 univ.** | **BLOCKER** |
| charge-authority-pack | OK | **404** | 200 (generic fallback) | OK | n/a | **400 Invalid product** | **BLOCKER** |
| precedent-watchlist | OK | **404** | 200 (generic fallback) | OK | 401 unauthed (guard intact) | **400 Invalid product** | **BLOCKER** |
| arrest-survival-kit | OK | 200 | 200 | OK | n/a | OK (real data) | PASS |

## CRITICAL findings

| # | Product | Failure | Evidence | Fix |
|---|---|---|---|---|
| C1 | charge-authority-pack | No dedicated landing route. `/charge-authority-pack` returns **404** in prod. Only `/services/charge-authority-pack` (generic fallback path with no AvailabilityChecker, no intake form) renders. Customers cannot complete a Tier 9 purchase flow because the AvailabilityChecker (which calls `/api/check-availability/[slug]`) is the surface that asks for `chargeType`. The generic fallback's only CTA is `/checkout?standaloneProduct=charge-authority-pack` which on the intake-form side requires intake fields keyed off the same checker the customer never sees. | `Glob src/app/charge-authority-pack/**` returns no files. `curl https://imnotanattorney.com/charge-authority-pack` → 404. Compare to dedicated pages at `src/app/district-court-intelligence/page.tsx`, `src/app/arrest-survival-kit/page.tsx`. | Build `src/app/charge-authority-pack/page.tsx` mirroring `src/app/district-court-intelligence/page.tsx` shape, mounting `<AvailabilityChecker slug="charge-authority-pack" .../>`. Block the live flip until shipped. |
| C2 | precedent-watchlist | Same shape as C1. `/precedent-watchlist` returns **404** in prod. Only `/services/precedent-watchlist` renders, generic fallback, no intake. Customers reach checkout via `?standaloneProduct=precedent-watchlist` but the intake fields the report needs (`chargeType`, optional `state`) cannot be collected pre-purchase, so the post-purchase intake page must collect them and the cron drip's seed depends on them. The drip is the product's value prop ($47 floor + 30-day weekly emails per `src/lib/products.ts:1352-1364`); without an intake-pre-purchase capture path the seed is fragile. | `Glob src/app/precedent-watchlist/**` returns no files. `curl https://imnotanattorney.com/precedent-watchlist` → 404. AvailabilityChecker `Slug` union at `src/components/tier9/AvailabilityChecker.tsx:207-212` excludes both `precedent-watchlist` and `charge-authority-pack`, so even adding the dedicated page wouldn't compile-clean without first widening the union. | Two-step: (a) widen `Slug` union in `AvailabilityChecker.tsx` to include `precedent-watchlist` + `charge-authority-pack` and add the per-slug intake-payload branches; (b) build `src/app/precedent-watchlist/page.tsx`. Both before re-flipping live. |
| C3 | charge-authority-pack + precedent-watchlist | `/api/check-availability/[slug]` has its OWN `TIER9_SLUGS` set (separate from `src/lib/tier9-reports/constants.ts`) that excludes both slugs. `curl POST .../api/check-availability/charge-authority-pack` → `400 {"error":"Invalid product"}`. `curl POST .../api/check-availability/precedent-watchlist` → `400 {"error":"Invalid product"}`. Even after C1+C2 fixes, the AvailabilityChecker call would 400. | `src/app/api/check-availability/[slug]/route.ts:25-32` defines a local `TIER9_SLUGS` Set with only 6 entries. The shared canonical set at `src/lib/tier9-reports/constants.ts:6-17` has 10 entries including all 4 audited slugs. Verified live with two prod POSTs. | Replace the local Set with `import { TIER9_SLUGS } from "@/lib/tier9-reports/constants"` and add the two missing case branches calling new `checkPrecedentWatchlistCoverage()` + `checkChargeAuthorityPackCoverage()` helpers in `src/lib/tier9-reports/coverage.ts`. |
| C4 | district-court-intelligence | Pre-purchase coverage helper `checkDistrictCoverage` (`src/lib/tier9-reports/coverage.ts:283-313`) queries `judge_demographics` ilike `district` against `safeStateName.toLowerCase()` (e.g. `%florida%`), but `judge_demographics.district` stores numeric district codes (`"30"`, `"40"`, `"73"`, `"90"` etc, NOT state names). Verified by direct PostgREST query at `judge_demographics.district` returning `"90"` for DC judges. The same query against `outcome_benchmarks.jurisdiction_name` ilike `%florida%` returns 0 because that table is national-only (every row `jurisdiction_level=national`, `jurisdiction_name="United States"`). Live POST to `/api/check-availability/district-court-intelligence` with state=FL returns `{"available":false,"coverage":{"judges":0,"benchmarks":0}}`. The same broken result applies to FL/TX/CA/NY/every state. The post-purchase resolver (`courthouse-intelligence.ts`) correctly uses `ussc_districts.state_code` and DOES find data, so the broken signal is **only** in the pre-purchase gate, but the AvailabilityChecker on `/district-court-intelligence` will surface "no coverage" pre-purchase across the board, which kills conversion. | `coverage.ts:295-300` ilike on `judge_demographics.district` + `outcome_benchmarks.jurisdiction_name`. Live PostgREST: `judge_demographics` sample district='90', `outcome_benchmarks` sample jurisdiction_level='national'. Live curl returned `available:false`. Resolver itself works: `ussc_districts` for FL returns 3 districts; `judge_disposition_profile` for those `cl_court_id` returns 56 judges; `motion_outcome_rates_by_circuit` for circuit='11' charge_type='(all)' returns 17 rows; `ussc_sentencing_all` for FL districts returns 37,601 rows. | Rewrite `checkDistrictCoverage(stateCode)` to mirror the resolver: lookup `ussc_districts` by `state_code`, count `judge_disposition_profile` by `district_id` IN (cl_court_ids), count `motion_outcome_rates_by_circuit` by normalized circuit. Same data, same expectation, same shape as the post-purchase report. |

## WARNING findings

| # | Product | Issue | Evidence | Fix |
|---|---|---|---|---|
| W1 | district-court-intelligence | `src/app/district-court-intelligence/page.tsx:1-7,18-44` still describes "$97 standalone Tier 9 data product" + headline copy still tied to JUSTFAIR-only "595,851 federal cases in your district" framing. The product was upgraded to $147 Courthouse Intelligence Pack 2026-04-23 per `src/lib/products.ts:1264-1284` but the dedicated page wasn't refreshed in the same PR. | File header comment + CHECK_ITEMS array. Sales SAMPLE_ROWS (lines 46+) hardcode "$97-era" framing with conviction-rate / plea-rate / 84-month median values; new $147 pack scope is judges + circuit motions + USSC district aggregates. | Rewrite hero, subhead, CHECK_ITEMS, and SAMPLE_ROWS to match the upgraded pack scope (`src/lib/tier9-reports/courthouse-intelligence.ts:1-37` is the spec). |
| W2 | precedent-watchlist | `src/app/api/cron/precedent-watchlist-emails/route.ts:61-67` has FORBIDDEN_FROM_DOMAINS guard, but the cron's actual `RESEND_FROM_EMAIL` env value should be verified against this list at deploy time (no test in the file). If the env was misconfigured, the guard fires at runtime per send-loop iteration but the customer still received the instant report — the drip just silently fails. | File guard exists; runtime probe-on-deploy doesn't. | Add a startup probe (or pre-cron health endpoint) that reads `RESEND_FROM_EMAIL` and logs a warning to Telegram if it matches the forbidden list. |
| W3 | charge-authority-pack | `CHARGE_TYPE_AUTHORITY_MAP` at `src/lib/charge-slug-maps.ts` (verified live) covers ~23 internal charge_types but only DUI / drug-trafficking / drug-possession-cocaine return non-empty `charge_type_top_authorities` rows in prod. Slugs like `theft`, `assault`, `rico` map to user slugs that have **zero** rows in `charge_type_top_authorities`, falling through to `citation_authority_criminal` national fallback. The fallback path adds a limitation note but the customer paid for charge-specific intelligence and got national. | Live PostgREST: `charge_type_top_authorities WHERE charge_type='theft'` → 0 rows; same for assault, rico. The 23 distinct charge_types in CTA are skewed toward DUI/drug variants. | Either (a) restrict the AvailabilityChecker chargeType picker to slugs that have ≥10 CTA rows, or (b) document the fallback prominently in the AvailabilityChecker banner before purchase. |
| W4 | arrest-survival-kit | Renderer at `src/lib/tier9-reports/render.ts:2200` correctly uses "use-of-force incident" copy. Sibling renderer at `render.ts:1316` (different product — Officer BG Check) still uses "fatal encounter" copy. Not a blocker for this audit, but the post-merge lint (commit 48a07021 "fix(dt4): apply PR #180 review findings") only swept the ASK path. | grep `fatal encounter` matched line 1316 in render.ts (not in renderArrestSurvivalKit at 2111). | Out-of-scope for ASK ship; track separately. Officer BG Check pre-existing copy. |
| W5 | precedent-watchlist | Per-purchase drip seed lives in `orders.watchlist_email_state` JSONB column (migration `20260423e_watchlist_email_state.sql` verified — column present, partial index on `started_at`). No standalone `watchlist_email_state` TABLE — that's the design. Cron route currently queries `orders.watchlist_email_state IS NOT NULL`. Concern: a user who buys precedent-watchlist before the intake-form path is built (C2) cannot have the JSONB seeded with `chargeType` because pre-purchase intake doesn't capture it. Drip will degrade silently. | Verified: `orders` row exposes `watchlist_email_state` column; row count NULL right now (no purchases yet). Schema OK; data flow needs intake. | Address as part of C2 (build dedicated landing + intake). |

## SUGGESTIONS

| # | Product | Issue | Evidence | Fix |
|---|---|---|---|---|
| S1 | All 4 | `src/components/tier9/AvailabilityChecker.tsx:211-212` `Slug` union currently only includes `district-court-intelligence`, `arrest-survival-kit`, `judge-report-card`, `officer-background-check`, `similar-cases-analyzer`, `federal-jury-instruction-brief`. Even after building dedicated pages for the missing 2, this union must widen or TypeScript blocks the new mount. | File grep matched 6 entries. | Widen Slug union; add per-slug intake-payload branches in the body where `slug === '...'` checks fork. |
| S2 | charge-authority-pack | The product description in `src/lib/products.ts:1294-1295` says "Top 10 must-cite defense authorities … 3-year citation velocity". Verified `citation_velocity_criminal` window is 24-month (constant `VELOCITY_WINDOW_MONTHS = 24` at `src/lib/tier9-reports/precedent-watchlist.ts:119`). Misalignment between product description ("3-year") and rendered data ("24 months") is small but a drift defendants may notice. | Two file constants disagree. | Pick one. The data pipeline is 24-month; update the product copy to match. |
| S3 | district-court-intelligence | `motion_outcome_rates_by_circuit` for circuit='11' charge_type='(all)' returns 17 rows. The resolver renders a "circuit motion grant rates (appellate-direction)" section. Defendants on a state-court case will assume these are state-trial-court motion rates, not federal-appellate. Section header copy must be unambiguous. | Verified in resolver `courthouse-intelligence.ts:264` comment: "this is circuit appellate direction, NOT trial-court grant rate". Worth re-reading the rendered HTML to confirm the disclaimer is prominent. | Read `renderCourthouseIntelligence` rendered HTML, ensure appellate disclaimer is not collapsed under a fold. Out-of-scope to fix without a render-test artifact. |

## Sales-page UPL spot-check

- `charge-authority-pack.ts` (renderer + spec): banned phrases ("you should", "consult your attorney", "we recommend", "we advise", "publicly available", "your best option") only appear in HEADER COMMENT enumerating the ban list. Rendered HTML uses clinical aggregate language. PASS.
- `precedent-watchlist.ts`: same. PASS.
- `courthouse-intelligence.ts`: zero matches for any banned phrase. PASS.
- `render.ts:2111-2240` (renderArrestSurvivalKit): zero matches. PASS.

## Cron auth + checkout slug + sitemap

- Cron `/api/cron/precedent-watchlist-emails` returns **401 Unauthorized** unauthed. Guard intact.
- Checkout `/api/checkout/route.ts:114-124` calls `isValidProduct(slug) && product.isActive` — both pass for all 4 (verified `isActive:true` + `live:true` consistent in `src/lib/products.ts:1261,1284,1304,1322,1364` and `src/lib/tiers.ts:325,341,357,394,410`).
- Sitemap `src/app/sitemap.ts:54-83` auto-includes all 4 via `productsByCategory("research")` filter (`p.isActive && p.category==="research"`). PASS.

## Recommendation order if you ship fixes

1. **C3** (5 min): widen `/api/check-availability` TIER9_SLUGS to import canonical constants + add 2 case branches. This unblocks Q&A on prod within 1 deploy.
2. **C4** (15 min): rewrite `checkDistrictCoverage` to mirror resolver. Same deploy. After this lands, `district-court-intelligence` is shippable.
3. **C1+C2+S1** (90 min): widen Slug union + build dedicated landing pages for `charge-authority-pack` + `precedent-watchlist` mirroring `district-court-intelligence/page.tsx` + `arrest-survival-kit/page.tsx` shape. Until this lands, both products are paid-purchaseable but reach customers as a generic fallback page; recommended to revert `live:true` → `live:false` for those two until landings ship.
4. **W1** (30 min): refresh `district-court-intelligence/page.tsx` copy to $147 Courthouse Pack scope.
5. **W3** (15 min): restrict AvailabilityChecker chargeType picker for charge-authority-pack OR add fallback-disclosure banner.
